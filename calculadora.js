var somar = function(x, y) {
    return x + y;
}

const subtrair = (x, y) => x - y;

var um = 1;
const dois = 2;
let tres = 3;

console.log("1 + 2 = ", somar(um, dois));
console.log("3 - 1 = ", subtrair(tres, um));